window.addEventListener("DOMContentLoaded", (event) => {
  const datatablesSimple = document.getElementById("EquipamentosTable");
  if (datatablesSimple) {
    let table = new DataTable(datatablesSimple);
  }
});
